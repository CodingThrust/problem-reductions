# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml
# Install: npx -y @smithery/cli install problemreductions --client claude

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({ command: 'pred', args: ['mcp'] })
